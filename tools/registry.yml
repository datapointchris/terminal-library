# Tool Registry
# What each tool is, why you would reach for it, and what to type.
#
# Reference, not declaration: what puts a tool on a machine is dotfiles'
# packages.yml, and the two describe largely different populations — most of
# what is documented here is a shell function or a script no package manager
# installs. Neither file is derivable from the other.
#
# DURING THE TOOLBOX RETIREMENT: dotfiles still carries the copy toolbox reads,
# at configs/common/.local/share/toolbox/registry.yml. doit reads this one. An
# edit here will not show up in `toolbox show` until that copy is deleted.

version: "1.0"
last_updated: "2026-07-16"

tools:
  # =================================================================
  # FILE MANAGEMENT & VIEWING
  # =================================================================

  bat:
    category: file-viewer
    description: "Syntax-highlighting cat replacement with git integration"
    installed_via: brew
    usage: "bat [options] <file>"
    why_use: "Beautiful syntax highlighting, line numbers, git diff integration, automatic paging"
    examples:
      - cmd: "bat README.md"
        desc: "View file with syntax highlighting"
      - cmd: "bat -n file.rs"
        desc: "Show with line numbers"
      - cmd: "bat --theme='gruvbox-dark' file.py"
        desc: "Use specific theme"
      - cmd: "bat --diff file.txt"
        desc: "Show git modifications"
    see_also: [eza, less, cat]
    tags: [cli, productivity, git, syntax-highlighting]
    docs_url: "https://github.com/sharkdp/bat"

  lnav:
    category: file-viewer
    description: "Log file navigator with format auto-detection, SQL queries, and live tail"
    installed_via: brew
    usage: "lnav [options] [logfile...]"
    why_use: "Auto-detects log formats, merges multiple logs by timestamp, SQL queries over log lines, live tail"
    examples:
      - cmd: "lnav /var/log/syslog"
        desc: "Open a single log file"
      - cmd: "lnav /var/log/*.log"
        desc: "Merge multiple logs by timestamp"
      - cmd: "lnav -c ';SELECT * FROM syslog_log WHERE log_level = \"error\"'"
        desc: "Query log lines with SQL"
    see_also: [bat, colored-log]
    tags: [cli, tui, logs, monitoring]
    docs_url: "https://lnav.org"

  eza:
    category: file-management
    description: "Modern ls replacement with git integration, colors, and icons"
    installed_via: brew
    usage: "eza [options] [path]"
    why_use: "Git status in listings, better colors, Nerd Font icons, tree view built-in"
    examples:
      - cmd: "eza -la"
        desc: "Long format with hidden files"
      - cmd: "eza --tree --level=2"
        desc: "Tree view, 2 levels deep"
      - cmd: "eza --git-ignore"
        desc: "Respect .gitignore"
      - cmd: "eza -T --git"
        desc: "Tree with git status"
      - cmd: "eza -lh --sort=modified"
        desc: "Sort by modification time"
    see_also: [bat, fd, tree, ls]
    tags: [cli, productivity, git, file-management]
    docs_url: "https://github.com/eza-community/eza"

  fd:
    category: search
    description: "Fast, user-friendly find alternative with sensible defaults"
    installed_via: brew
    usage: "fd [pattern] [path]"
    why_use: "Faster than find, respects .gitignore, colorized output, simpler syntax"
    examples:
      - cmd: "fd config"
        desc: "Find files/dirs matching 'config'"
      - cmd: "fd -e md"
        desc: "Find all markdown files"
      - cmd: "fd -t f 'test.*\\.py$'"
        desc: "Find Python test files"
      - cmd: "fd -H -I config"
        desc: "Include hidden and ignored files"
      - cmd: "fd . ~/dotfiles -x bat"
        desc: "Find and execute command on each file"
    see_also: [ripgrep, fzf, find]
    tags: [cli, search, productivity]
    docs_url: "https://github.com/sharkdp/fd"

  yazi:
    category: file-management
    description: "Blazing fast terminal file manager with image preview"
    installed_via: brew
    usage: "yazi [path]"
    why_use: "Fast, supports image preview, miller columns, vim keybindings, async I/O"
    examples:
      - cmd: "yazi"
        desc: "Open in current directory"
      - cmd: "yazi ~/dotfiles"
        desc: "Open specific directory"
      - cmd: "yy"
        desc: "Shell function: cd to directory on exit"
    see_also: [eza, fd, ranger, lf]
    tags: [cli, file-management, tui, productivity]
    docs_url: "https://yazi-rs.github.io/"

  # =================================================================
  # SEARCH & TEXT PROCESSING
  # =================================================================

  ripgrep:
    category: search
    description: "Ultra-fast recursive grep with smart defaults"
    installed_via: dotfiles
    usage: "rg [pattern] [path]"
    why_use: "Fastest grep tool, respects .gitignore, supports PCRE2, multiline search"
    examples:
      - cmd: "rg 'function'"
        desc: "Search for pattern in current directory"
      - cmd: "rg -t py 'import'"
        desc: "Search only Python files"
      - cmd: "rg -i 'error' --context 3"
        desc: "Case-insensitive with 3 lines context"
      - cmd: "rg 'TODO' -g '!target'"
        desc: "Search excluding target directory"
      - cmd: "rg -U 'struct.*field'"
        desc: "Multiline search"
    see_also: [fd, fzf, grep, ag]
    tags: [cli, search, productivity, regex]
    docs_url: "https://github.com/BurntSushi/ripgrep"

  fzf:
    category: search
    description: "Command-line fuzzy finder for files, history, and more"
    installed_via: brew
    usage: "fzf [options]"
    why_use: "Interactive filtering, integrates with shell history, vim, git, etc."
    examples:
      - cmd: "CTRL-T"
        desc: "Fuzzy find files (shell keybinding)"
      - cmd: "CTRL-R"
        desc: "Fuzzy search command history"
      - cmd: "ALT-C"
        desc: "Fuzzy cd to directory"
      - cmd: "git log | fzf"
        desc: "Interactive git log browser"
      - cmd: "fd -t f | fzf --preview 'bat --color=always {}'"
        desc: "File finder with preview"
    see_also: [fd, ripgrep, zoxide]
    tags: [cli, search, productivity, interactive]
    docs_url: "https://github.com/junegunn/fzf"

  jq:
    category: text-processing
    description: "Command-line JSON processor and pretty-printer"
    installed_via: brew
    usage: "jq [filter] [file]"
    why_use: "Parse, transform, and query JSON data from CLI"
    examples:
      - cmd: "echo '{\"name\":\"Chris\"}' | jq '.name'"
        desc: "Extract field from JSON"
      - cmd: "cat data.json | jq '.[]'"
        desc: "Iterate array elements"
      - cmd: "jq -r '.items[].name' data.json"
        desc: "Raw output (no quotes)"
      - cmd: "curl api.example.com | jq '.'"
        desc: "Pretty-print API response"
    see_also: [yq, curl]
    tags: [cli, json, text-processing]
    docs_url: "https://jqlang.github.io/jq/"

  # =================================================================
  # NAVIGATION
  # =================================================================

  broot:
    category: navigation
    description: "Interactive directory tree navigator with fuzzy search and cd-on-exit"
    installed_via: dotfiles
    usage: "br [path]"
    why_use: "Navigate large trees with fuzzy filtering without losing your place; opens files in $EDITOR; can cd to selected directory on exit"
    examples:
      - cmd: "br"
        desc: "Open tree in current directory"
      - cmd: "br ~/projects"
        desc: "Open tree at specific path"
      - cmd: "br -h"
        desc: "Show hidden files"
      - cmd: "br -s"
        desc: "Show file sizes"
      - cmd: "br -d"
        desc: "Show last-modified dates"
      - cmd: "Type to filter, Enter to descend, Alt+Enter to cd and exit"
        desc: "Core navigation pattern"
      - cmd: ":e"
        desc: "Open selected file in $EDITOR"
    see_also: [zoxide, yazi, fd, eza]
    tags: [cli, tui, navigation, file-management]
    docs_url: "https://dystroy.org/broot/"

  zoxide:
    category: navigation
    description: "Smarter cd command that learns your habits"
    installed_via: brew
    usage: "z [pattern]"
    why_use: "Jump to frequently-used directories by partial match, no need for full paths"
    examples:
      - cmd: "z dot"
        desc: "Jump to ~/dotfiles (if used frequently)"
      - cmd: "z doc"
        desc: "Jump to most-used directory matching 'doc'"
      - cmd: "zi doc"
        desc: "Interactive selection if multiple matches"
      - cmd: "z -"
        desc: "Go to previous directory"
    see_also: [cd, autojump, fasd]
    tags: [cli, navigation, productivity]
    docs_url: "https://github.com/ajeetdsouza/zoxide"

  # =================================================================
  # VERSION CONTROL
  # =================================================================

  git:
    category: version-control
    description: "Distributed version control system"
    installed_via: brew
    usage: "git [command] [options]"
    why_use: "Industry-standard version control, required for most development workflows"
    examples:
      - cmd: "git status"
        desc: "Show working tree status"
      - cmd: "git add -p"
        desc: "Interactively stage changes"
      - cmd: "git log --oneline --graph"
        desc: "Compact history with graph"
      - cmd: "git diff --staged"
        desc: "Show staged changes"
    see_also: [gh, lazygit, git-delta]
    tags: [vcs, development, essential]
    docs_url: "https://git-scm.com/"

  gh:
    category: version-control
    description: "GitHub CLI for pull requests, issues, and workflows"
    installed_via: brew
    usage: "gh [command] [options]"
    why_use: "Manage GitHub from terminal, create PRs, view issues, run workflows"
    examples:
      - cmd: "gh pr create"
        desc: "Create pull request interactively"
      - cmd: "gh pr list"
        desc: "List pull requests"
      - cmd: "gh pr checkout 123"
        desc: "Checkout PR #123 locally"
      - cmd: "gh repo view"
        desc: "View repository details"
      - cmd: "gh workflow run"
        desc: "Trigger GitHub Actions workflow"
    see_also: [git, lazygit]
    tags: [vcs, github, development]
    docs_url: "https://cli.github.com/"

  jira:
    category: version-control
    description: "Jira issues, sprints, and transitions from the terminal"
    installed_via: go
    usage: "jira issue VERB [KEY] [OPTIONS]"
    why_use: "The issue half of the work loop that bbkt drives the pull request half of — an interactive table by default, so browsing a sprint does not mean loading the web UI"
    examples:
      - cmd: "jira init"
        desc: "One-time setup — choose Cloud, then site URL, email, and API token"
      - cmd: "jira issue list -a$(jira me) -sopen"
        desc: "Your open issues, in the interactive table"
      - cmd: "jira sprint list --current -a$(jira me)"
        desc: "What is assigned to you in the sprint running now"
      - cmd: "jira issue view PROJ-123"
        desc: "Full issue with description, comments, and linked development work"
      - cmd: "jira issue move PROJ-123 'In Progress'"
        desc: "Transition an issue without opening a browser"
      - cmd: "jira issue list --plain --no-headers"
        desc: "Plain output for piping — the table is interactive by default"
    see_also: [bbkt, gh]
    tags: [jira, issue-tracking, work, go, cli]
    docs_url: "https://github.com/ankitpokhrel/jira-cli"

  lazygit:
    category: version-control
    description: "Terminal UI for git with intuitive keybindings"
    installed_via: brew
    usage: "lazygit"
    why_use: "Visual git interface, stage hunks easily, interactive rebase, branch management"
    examples:
      - cmd: "lazygit"
        desc: "Open in current repository"
      - cmd: "lazygit -p /path/to/repo"
        desc: "Open specific repository"
    see_also: [git, gh, git-delta]
    tags: [vcs, tui, development, productivity]
    docs_url: "https://github.com/jesseduffield/lazygit"

  git-delta:
    category: version-control
    description: "Syntax-highlighting pager for git, diff, and grep output"
    installed_via: dotfiles
    usage: "delta [file]"
    why_use: "Beautiful git diffs with syntax highlighting, line numbers, side-by-side view"
    examples:
      - cmd: "git diff | delta"
        desc: "View diff with delta (configured in .gitconfig)"
      - cmd: "git log -p | delta"
        desc: "View commit patches with delta"
      - cmd: "git show HEAD | delta"
        desc: "Show last commit with delta"
    see_also: [git, bat, diff]
    tags: [vcs, syntax-highlighting, git]
    docs_url: "https://github.com/dandavison/delta"

  # Forgit - Interactive Git Commands with fzf
  # Installed via: task shell:install

  git-forgit-log:
    category: version-control
    description: "Interactive git log viewer with fzf (alias: glo)"
    installed_via: dotfiles
    usage: "git forgit log"
    why_use: "Browse git history interactively, preview commits, search by content"
    examples:
      - cmd: "git forgit log"
        desc: "Interactive commit history browser"
    see_also: [git, lazygit, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-reflog:
    category: version-control
    description: "Interactive git reflog viewer with fzf (alias: grl)"
    installed_via: dotfiles
    usage: "git forgit reflog"
    why_use: "Browse reflog interactively to recover lost commits or understand history changes"
    examples:
      - cmd: "git forgit reflog"
        desc: "Interactive reflog browser"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-diff:
    category: version-control
    description: "Interactive git diff viewer with fzf (alias: gd)"
    installed_via: dotfiles
    usage: "git forgit diff"
    why_use: "Interactively select files to view diffs with syntax highlighting"
    examples:
      - cmd: "git forgit diff"
        desc: "Interactive diff file selector"
    see_also: [git, git-delta, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  fleet:
    category: version-control
    description: "Reports across the whole repo portfolio, including every open PR"
    installed_via: go
    usage: "fleet <noun> <verb>"
    why_use: "One question about all the repos at once — forge acts on repos one at a time, fleet reads across them"
    examples:
      - cmd: "prs"
        desc: "Alias for `fleet prs review` — pick an open PR, open its diff in a window"
      - cmd: "fleet prs list"
        desc: "Every open PR across the portfolio, oldest first"
      - cmd: "fleet status"
        desc: "What each repo's planning says"
    see_also: [gh, gh-dash, forge, git-delta]
    tags: [git, github, pr, review, portfolio, fleet]

  git-forgit-show:
    category: version-control
    description: "Interactive git show commit viewer with fzf (alias: gso)"
    installed_via: dotfiles
    usage: "git forgit show"
    why_use: "Interactively select and view commit details with diffs"
    examples:
      - cmd: "git forgit show"
        desc: "Interactive commit viewer"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-add:
    category: version-control
    description: "Interactive git add file selector with fzf (alias: ga)"
    installed_via: dotfiles
    usage: "git forgit add"
    why_use: "Visually select files to stage with preview of changes"
    examples:
      - cmd: "git forgit add"
        desc: "Interactive file staging"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, staging]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-reset-head:
    category: version-control
    description: "Interactive git reset HEAD file selector with fzf (alias: grh)"
    installed_via: dotfiles
    usage: "git forgit reset_head"
    why_use: "Interactively unstage files with preview"
    examples:
      - cmd: "git forgit reset_head"
        desc: "Interactive unstaging"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, staging]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-ignore:
    category: version-control
    description: "Interactive .gitignore generator with fzf (alias: gi)"
    installed_via: dotfiles
    usage: "git forgit ignore"
    why_use: "Add gitignore patterns from gitignore.io templates interactively"
    examples:
      - cmd: "git forgit ignore"
        desc: "Generate .gitignore entries"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-checkout-file:
    category: version-control
    description: "Interactive git checkout file selector with fzf (alias: gcf)"
    installed_via: dotfiles
    usage: "git forgit checkout_file"
    why_use: "Interactively select files to discard changes with preview"
    examples:
      - cmd: "git forgit checkout_file"
        desc: "Interactive file checkout/discard"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-checkout-branch:
    category: version-control
    description: "Interactive git branch checkout selector with fzf (alias: gcb)"
    installed_via: dotfiles
    usage: "git forgit checkout_branch"
    why_use: "Fuzzy search and switch branches with preview of branch info"
    examples:
      - cmd: "git forgit checkout_branch"
        desc: "Interactive branch switching"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, branches]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-checkout-tag:
    category: version-control
    description: "Interactive git tag checkout selector with fzf (alias: gct)"
    installed_via: dotfiles
    usage: "git forgit checkout_tag"
    why_use: "Interactively select and checkout tags"
    examples:
      - cmd: "git forgit checkout_tag"
        desc: "Interactive tag checkout"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, tags]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-checkout-commit:
    category: version-control
    description: "Interactive git commit checkout selector with fzf (alias: gco)"
    installed_via: dotfiles
    usage: "git forgit checkout_commit"
    why_use: "Interactively select and checkout specific commits"
    examples:
      - cmd: "git forgit checkout_commit"
        desc: "Interactive commit checkout"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-branch-delete:
    category: version-control
    description: "Interactive git branch delete selector with fzf (alias: gbd)"
    installed_via: dotfiles
    usage: "git forgit branch_delete"
    why_use: "Safely delete branches with interactive selection and confirmation"
    examples:
      - cmd: "git forgit branch_delete"
        desc: "Interactive branch deletion"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, branches]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-revert-commit:
    category: version-control
    description: "Interactive git revert commit selector with fzf (alias: grc)"
    installed_via: dotfiles
    usage: "git forgit revert_commit"
    why_use: "Interactively select commits to revert with preview"
    examples:
      - cmd: "git forgit revert_commit"
        desc: "Interactive commit revert"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-clean:
    category: version-control
    description: "Interactive git clean untracked files selector with fzf (alias: gclean)"
    installed_via: dotfiles
    usage: "git forgit clean"
    why_use: "Safely remove untracked files with preview and selection"
    examples:
      - cmd: "git forgit clean"
        desc: "Interactive untracked file removal"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-stash-show:
    category: version-control
    description: "Interactive git stash viewer with fzf (alias: gss)"
    installed_via: dotfiles
    usage: "git forgit stash_show"
    why_use: "Browse and view stash entries interactively with diff preview"
    examples:
      - cmd: "git forgit stash_show"
        desc: "Interactive stash browser"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, stash]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-stash-push:
    category: version-control
    description: "Interactive git stash push file selector with fzf (alias: gsp)"
    installed_via: dotfiles
    usage: "git forgit stash_push"
    why_use: "Selectively stash files with interactive selection"
    examples:
      - cmd: "git forgit stash_push"
        desc: "Interactive selective stashing"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, stash]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-cherry-pick:
    category: version-control
    description: "Interactive git cherry-pick commit selector with fzf (alias: gcp)"
    installed_via: dotfiles
    usage: "git forgit cherry_pick"
    why_use: "Interactively select commits to cherry-pick from branches"
    examples:
      - cmd: "git forgit cherry_pick"
        desc: "Interactive cherry-pick"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-rebase:
    category: version-control
    description: "Interactive git rebase -i commit selector with fzf (alias: grb)"
    installed_via: dotfiles
    usage: "git forgit rebase"
    why_use: "Interactively select base commit for interactive rebase"
    examples:
      - cmd: "git forgit rebase"
        desc: "Interactive rebase selector"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, rebase]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-blame:
    category: version-control
    description: "Interactive git blame viewer with fzf (alias: gbl)"
    installed_via: dotfiles
    usage: "git forgit blame"
    why_use: "Interactively view file blame with commit details"
    examples:
      - cmd: "git forgit blame"
        desc: "Interactive blame viewer"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-fixup:
    category: version-control
    description: "Interactive commit fixup with autosquash (alias: gfu)"
    installed_via: dotfiles
    usage: "git forgit fixup"
    why_use: "Interactively create fixup commits for previous commits"
    examples:
      - cmd: "git forgit fixup"
        desc: "Interactive fixup commit creation"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, rebase]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-squash:
    category: version-control
    description: "Interactive commit squash with autosquash (alias: gsq)"
    installed_via: dotfiles
    usage: "git forgit squash"
    why_use: "Interactively create squash commits for combining with previous commits"
    examples:
      - cmd: "git forgit squash"
        desc: "Interactive squash commit creation"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, rebase]
    docs_url: "https://github.com/wfxr/forgit"

  git-forgit-reword:
    category: version-control
    description: "Interactive commit reword with autosquash (alias: grw)"
    installed_via: dotfiles
    usage: "git forgit reword"
    why_use: "Interactively reword previous commit messages"
    examples:
      - cmd: "git forgit reword"
        desc: "Interactive commit message editing"
    see_also: [git, fzf]
    tags: [vcs, git, interactive, fzf, rebase]
    docs_url: "https://github.com/wfxr/forgit"

  # =================================================================
  # DEVELOPMENT TOOLS
  # =================================================================

  neovim:
    category: editor
    description: "Hyperextensible Vim-based text editor"
    installed_via: dotfiles
    usage: "nvim [file]"
    why_use: "Modern vim, Lua configuration, LSP support, async plugins, active ecosystem"
    examples:
      - cmd: "nvim file.py"
        desc: "Edit file"
      - cmd: "nvim -d file1 file2"
        desc: "Diff mode"
      - cmd: "nvim +PluginUpdate +qa"
        desc: "Update plugins and quit"
    see_also: [vim, code]
    tags: [editor, development, essential, vim]
    docs_url: "https://neovim.io/"

  tmux:
    category: terminal-multiplexer
    description: "Terminal multiplexer for persistent sessions"
    installed_via: brew
    usage: "tmux [command]"
    why_use: "Persistent sessions, split panes, multiple windows, session sharing"
    examples:
      - cmd: "tmux"
        desc: "Start new session"
      - cmd: "tmux attach"
        desc: "Attach to existing session"
      - cmd: "tmux ls"
        desc: "List sessions"
      - cmd: "tmux new -s work"
        desc: "Create named session"
    see_also: [screen, zellij]
    tags: [terminal, productivity, multiplexer]
    docs_url: "https://github.com/tmux/tmux"

  # =================================================================
  # LANGUAGE VERSION MANAGERS
  # =================================================================

  uv:
    category: language-manager
    description: "Extremely fast Python package and project manager"
    installed_via: cargo
    usage: "uv [command] [options]"
    why_use: "Replaces pip, pipx, pyenv, virtualenv with one fast tool written in Rust"
    examples:
      - cmd: "uv venv"
        desc: "Create virtual environment"
      - cmd: "uv pip install requests"
        desc: "Install package (10-100x faster than pip)"
      - cmd: "uv tool install ruff"
        desc: "Install Python CLI tool globally"
      - cmd: "uv python install 3.12"
        desc: "Install Python version"
      - cmd: "uv run python script.py"
        desc: "Run script in managed environment"
    see_also: [pip, pyenv, virtualenv, pipx]
    tags: [python, package-manager, version-manager]
    docs_url: "https://github.com/astral-sh/uv"

  nvm:
    category: language-manager
    description: "Node.js version manager for switching Node versions"
    installed_via: dotfiles
    usage: "nvm [command] [version]"
    why_use: "Manage multiple Node.js versions, switch per project, no sudo needed"
    examples:
      - cmd: "nvm install --lts"
        desc: "Install latest LTS Node.js"
      - cmd: "nvm use 20"
        desc: "Switch to Node 20"
      - cmd: "nvm current"
        desc: "Show current version"
      - cmd: "nvm alias default 22"
        desc: "Set default Node version"
    see_also: [npm, node, fnm]
    tags: [nodejs, version-manager, development]
    docs_url: "https://github.com/nvm-sh/nvm"

  direnv:
    category: language-manager
    description: "Per-directory environment variables, auto-loaded on cd"
    installed_via: brew
    usage: "direnv [command]"
    why_use: "Automatic environment switching per project, no manual source/export"
    examples:
      - cmd: "direnv allow"
        desc: "Allow .envrc in current directory"
      - cmd: "direnv deny"
        desc: "Revoke .envrc permissions"
      - cmd: "direnv status"
        desc: "Show current direnv state"
      - cmd: "echo 'export FOO=bar' > .envrc && direnv allow"
        desc: "Create and activate environment"
    see_also: [nvm, uv, dotenv]
    tags: [environment, shell, development, automation]
    docs_url: "https://direnv.net/"

  # =================================================================
  # PYTHON TOOLS
  # =================================================================

  ruff:
    category: linter-formatter
    description: "Extremely fast Python linter and formatter written in Rust"
    installed_via: uv
    usage: "ruff [command] [path]"
    why_use: "Replaces flake8, black, isort, etc. with one fast tool (10-100x faster)"
    examples:
      - cmd: "ruff check ."
        desc: "Lint current directory"
      - cmd: "ruff format ."
        desc: "Format code"
      - cmd: "ruff check --fix ."
        desc: "Auto-fix violations"
      - cmd: "ruff check --select E,W ."
        desc: "Check specific rule categories"
    see_also: [black, flake8, isort, mypy]
    tags: [python, linter, formatter, development]
    docs_url: "https://github.com/astral-sh/ruff"

  mypy:
    category: linter-formatter
    description: "Static type checker for Python"
    installed_via: uv
    usage: "mypy [options] [files]"
    why_use: "Catch type errors before runtime, improve code quality and IDE support"
    examples:
      - cmd: "mypy script.py"
        desc: "Type-check file"
      - cmd: "mypy --strict ."
        desc: "Strict type checking"
      - cmd: "mypy --install-types"
        desc: "Install missing type stubs"
    see_also: [ruff, basedpyright, pyright]
    tags: [python, type-checker, development]
    docs_url: "https://mypy-lang.org/"

  basedpyright:
    category: linter-formatter
    description: "Type checker for Python (fork of Pyright with enhanced features)"
    installed_via: uv
    usage: "basedpyright [options] [path]"
    why_use: "Fast type checking, good IDE integration, community-enhanced fork"
    examples:
      - cmd: "basedpyright ."
        desc: "Type-check current directory"
      - cmd: "basedpyright --watch"
        desc: "Watch mode for continuous checking"
    see_also: [mypy, pyright, ruff]
    tags: [python, type-checker, development]
    docs_url: "https://github.com/DetachHead/basedpyright"

  # =================================================================
  # NODE/JS TOOLS
  # =================================================================

  prettier:
    category: linter-formatter
    description: "Opinionated code formatter for JavaScript, TypeScript, and more"
    installed_via: npm
    usage: "prettier [options] [files]"
    why_use: "Automatic consistent code formatting, supports many languages"
    examples:
      - cmd: "prettier --write ."
        desc: "Format all files in directory"
      - cmd: "prettier --check ."
        desc: "Check if files are formatted"
      - cmd: "prettier --write 'src/**/*.{js,ts,md}'"
        desc: "Format specific file types"
    see_also: [prettierd, eslint]
    tags: [javascript, typescript, formatter, development]
    docs_url: "https://prettier.io/"

  eslint:
    category: linter-formatter
    description: "Pluggable JavaScript and TypeScript linter"
    installed_via: npm
    usage: "eslint [options] [files]"
    why_use: "Catch errors and enforce coding standards in JS/TS code"
    examples:
      - cmd: "eslint ."
        desc: "Lint current directory"
      - cmd: "eslint --fix ."
        desc: "Auto-fix violations"
      - cmd: "eslint --ext .ts,.tsx src/"
        desc: "Lint TypeScript files"
    see_also: [prettier, tsc]
    tags: [javascript, typescript, linter, development]
    docs_url: "https://eslint.org/"

  # =================================================================
  # LANGUAGE SERVERS (LSP)
  # =================================================================

  typescript-language-server:
    category: language-server
    description: "TypeScript/JavaScript LSP for code intelligence"
    installed_via: npm
    usage: "typescript-language-server --stdio"
    why_use: "Enables IDE features in editors: autocomplete, go-to-definition, etc."
    examples:
      - cmd: "Configured in Neovim's LSP setup"
        desc: "Used automatically by editor"
    see_also: [vscode-langservers-extracted, eslint]
    tags: [lsp, typescript, javascript, development]
    docs_url: "https://github.com/typescript-language-server/typescript-language-server"

  bash-language-server:
    category: language-server
    description: "Bash LSP for shell script intelligence"
    installed_via: npm
    usage: "bash-language-server start"
    why_use: "Shell script linting, completion, and navigation in editor"
    examples:
      - cmd: "Configured in Neovim's LSP setup"
        desc: "Used automatically by editor"
    see_also: [shellcheck, shfmt]
    tags: [lsp, bash, shell, development]
    docs_url: "https://github.com/bash-lsp/bash-language-server"

  yaml-language-server:
    category: language-server
    description: "YAML LSP with schema validation"
    installed_via: npm
    usage: "yaml-language-server --stdio"
    why_use: "YAML validation, autocomplete, schema support"
    examples:
      - cmd: "Configured in Neovim's LSP setup"
        desc: "Used automatically for .yml files"
    see_also: [json-language-server]
    tags: [lsp, yaml, development]
    docs_url: "https://github.com/redhat-developer/yaml-language-server"

  # =================================================================
  # INFRASTRUCTURE & DEVOPS
  # =================================================================

  docker:
    category: containerization
    description: "Platform for building and running containerized applications"
    installed_via: cask
    usage: "docker [command] [options]"
    why_use: "Standard for containerization, consistent dev environments, deployment"
    examples:
      - cmd: "docker ps"
        desc: "List running containers"
      - cmd: "docker build -t myapp ."
        desc: "Build image from Dockerfile"
      - cmd: "docker run -it myapp bash"
        desc: "Run container interactively"
      - cmd: "docker logs -f container_name"
        desc: "Follow container logs"
    see_also: [docker-compose, lazydocker, oxker]
    tags: [devops, containers, infrastructure]
    docs_url: "https://docs.docker.com/"

  lazydocker:
    category: containerization
    description: "Terminal UI for managing Docker containers and images"
    installed_via: brew
    usage: "lazydocker"
    why_use: "Visual interface for Docker, easy container management, log viewing"
    examples:
      - cmd: "lazydocker"
        desc: "Open Docker TUI"
    see_also: [docker, oxker, docker-compose]
    tags: [devops, containers, tui]
    docs_url: "https://github.com/jesseduffield/lazydocker"

  terraform:
    category: infrastructure
    description: "Infrastructure as Code tool for cloud resources"
    installed_via: brew
    usage: "terraform [command] [options]"
    why_use: "Declarative infrastructure, multi-cloud support, state management"
    examples:
      - cmd: "terraform init"
        desc: "Initialize working directory"
      - cmd: "terraform plan"
        desc: "Show execution plan"
      - cmd: "terraform apply"
        desc: "Apply infrastructure changes"
      - cmd: "terraform destroy"
        desc: "Destroy managed infrastructure"
    see_also: [terraform-docs, tflint, terraformer]
    tags: [devops, infrastructure, iac]
    docs_url: "https://www.terraform.io/"

  goose:
    category: infrastructure
    description: "Database migration tool with support for SQL and Go migrations"
    installed_via: dotfiles
    usage: "goose [command] [options]"
    why_use: "Simple, fast database migrations with multiple database support (PostgreSQL, MySQL, SQLite, etc.)"
    examples:
      - cmd: "goose -dir migrations postgres 'user=postgres dbname=mydb' up"
        desc: "Run all pending migrations"
      - cmd: "goose -dir migrations postgres 'user=postgres dbname=mydb' down"
        desc: "Rollback last migration"
      - cmd: "goose -dir migrations postgres 'user=postgres dbname=mydb' status"
        desc: "Show migration status"
      - cmd: "goose create add_users_table sql"
        desc: "Create new SQL migration file"
      - cmd: "goose -dir migrations postgres 'user=postgres dbname=mydb' version"
        desc: "Show current migration version"
    see_also: [docker, lazydocker]
    tags: [database, migrations, devops, sql]
    docs_url: "https://github.com/pressly/goose"

  psql:
    category: infrastructure
    description: "PostgreSQL interactive terminal for database connections and queries"
    installed_via: brew
    usage: "psql [options] [dbname [username]]"
    why_use: "Standard CLI for PostgreSQL - run queries, manage databases, import/export data"
    examples:
      - cmd: "psql -h localhost -U postgres -d mydb"
        desc: "Connect to local database"
      - cmd: "psql postgres://user:pass@host:5432/dbname"
        desc: "Connect using connection string"
      - cmd: "psql -c 'SELECT version();' -d mydb"
        desc: "Run single query"
      - cmd: "psql -f schema.sql -d mydb"
        desc: "Execute SQL file"
      - cmd: "pg_dump mydb > backup.sql"
        desc: "Export database to SQL file"
      - cmd: "pg_restore -d mydb backup.dump"
        desc: "Restore from dump file"
    see_also: [goose, docker, lazydocker]
    tags: [database, postgresql, sql, devops]
    docs_url: "https://www.postgresql.org/docs/current/app-psql.html"

  gofumpt:
    category: linter-formatter
    description: "Stricter gofmt formatter enforcing additional style rules"
    installed_via: dotfiles
    usage: "gofumpt [flags] [path ...]"
    why_use: "Stricter than gofmt with extra formatting rules for cleaner, more consistent Go code"
    examples:
      - cmd: "gofumpt -w ."
        desc: "Format all Go files in current directory"
      - cmd: "gofumpt -l ."
        desc: "List files that need formatting"
      - cmd: "gofumpt -d file.go"
        desc: "Show diff of formatting changes"
      - cmd: "gofumpt -extra -w ."
        desc: "Apply extra formatting rules"
    see_also: [shfmt, prettier, ruff]
    tags: [go, formatter, linter, development]
    docs_url: "https://github.com/mvdan/gofumpt"

  # =================================================================
  # TASK AUTOMATION
  # =================================================================

  task:
    category: automation
    description: "Modern task runner and build tool (Taskfile)"
    installed_via: brew
    usage: "task [task-name]"
    why_use: "Better than Makefiles, cross-platform, YAML-based, dependency management"
    examples:
      - cmd: "task"
        desc: "Run default task"
      - cmd: "task --list"
        desc: "List available tasks"
      - cmd: "task install"
        desc: "Run install task"
      - cmd: "task build --force"
        desc: "Force rebuild"
    see_also: [make, just]
    tags: [automation, build-tool, productivity]
    docs_url: "https://taskfile.dev/"

  watchexec:
    category: automation
    description: "Run a command when files change"
    installed_via: cargo
    usage: "watchexec [-c] -w <path> -- <command>"
    why_use: "Turns any command you already know into a live view — a test on save, a diff as it is written, a document as it grows"
    examples:
      - cmd: "watchexec -c -w src -- pytest -x"
        desc: "Re-run tests on every save, clearing between runs"
      - cmd: "watchexec -c -w \"$WT\" -w \"$(git -C \"$WT\" rev-parse --git-dir)/index\" -- \"git -C $WT diff origin/main | delta --paging=never\""
        desc: "Live diff of a worktree someone else is writing; watching the index is what catches their commits"
      - cmd: "watchexec -c -w ./docs -- 'glow -w $COLUMNS ./docs/latest.md'"
        desc: "Re-render a document as it is written"
    see_also: [task, entr]
    tags: [automation, file-watching, productivity]
    docs_url: "https://watchexec.github.io/"

  # =================================================================
  # SYSTEM MONITORING
  # =================================================================

  htop:
    category: system-monitoring
    description: "Interactive process viewer and system monitor"
    installed_via: brew
    usage: "htop"
    why_use: "Better than top, colorized output, mouse support, tree view"
    examples:
      - cmd: "htop"
        desc: "Monitor system processes"
      - cmd: "htop -u chris"
        desc: "Show only user's processes"
    see_also: [top, btop]
    tags: [system, monitoring, tui]
    docs_url: "https://htop.dev/"

  btop:
    category: system-monitoring
    description: "Resource monitor with CPU, memory, disks, network, and process views"
    installed_via: brew
    usage: "btop [options]"
    why_use: "Beautiful UI, themeable colors, more metrics than htop, GPU monitoring"
    examples:
      - cmd: "btop"
        desc: "Launch resource monitor"
      - cmd: "btop --utf-force"
        desc: "Force UTF-8 mode"
      - cmd: "btop -t"
        desc: "Start with tree view for processes"
    see_also: [htop, top, glances]
    tags: [system, monitoring, tui, themeable]
    docs_url: "https://github.com/aristocratos/btop"

  gdu:
    category: system-monitoring
    description: "Fast disk usage analyzer with interactive TUI for finding space hogs"
    installed_via: go
    usage: "gdu [directory]"
    why_use: "Parallel scanning optimized for SSDs, interactive navigation, delete files directly"
    examples:
      - cmd: "gdu"
        desc: "Analyze current directory"
      - cmd: "gdu ~"
        desc: "Analyze home directory"
      - cmd: "gdu -d"
        desc: "Show all mounted disks"
      - cmd: "gdu -n /path"
        desc: "Non-interactive mode (sorted list)"
      - cmd: "gdu -i /ignore/path /path"
        desc: "Ignore specific directories"
    see_also: [htop, btop, du, ncdu]
    tags: [disk-usage, tui, interactive, storage]
    docs_url: "https://github.com/dundee/gdu"

  # =================================================================
  # SHELL & FORMATTING
  # =================================================================

  shellcheck:
    category: linter-formatter
    description: "Static analysis tool for shell scripts"
    installed_via: brew
    usage: "shellcheck [file]"
    why_use: "Catch common shell script errors, suggest best practices"
    examples:
      - cmd: "shellcheck script.sh"
        desc: "Check shell script"
      - cmd: "shellcheck -x script.sh"
        desc: "Follow sourced files"
      - cmd: "shellcheck -S warning *.sh"
        desc: "Show warnings and above"
    see_also: [shfmt, bash-language-server]
    tags: [shell, linter, development]
    docs_url: "https://www.shellcheck.net/"

  shfmt:
    category: linter-formatter
    description: "Shell script formatter (bash, zsh, etc.)"
    installed_via: brew
    usage: "shfmt [options] [files]"
    why_use: "Automatic shell script formatting, consistent style"
    examples:
      - cmd: "shfmt -w script.sh"
        desc: "Format script in-place"
      - cmd: "shfmt -i 2 -d ."
        desc: "Check formatting with 2-space indent"
      - cmd: "shfmt -l -w ."
        desc: "Format all shell files"
    see_also: [shellcheck, prettier]
    tags: [shell, formatter, development]
    docs_url: "https://github.com/mvdan/sh"

  # =================================================================
  # CUSTOM DOTFILES TOOLS
  # =================================================================

  doit:
    category: custom-tools
    description: "What to do now, and everything that decides it — search, dashboard, cadence and practice over everything you own"
    installed_via: uv
    usage: "doit <command>"
    why_use: "One front door to the whole kit: `find` searches every collection you have, `next` draws from the weights you declared, and `dashboard` reads every app at once. It stewards no records of its own — each domain CLI stays the owner"
    examples:
      - cmd: "doit next"
        desc: "What to do now, drawn from your weighted pursuits"
      - cmd: "doit find keybind"
        desc: "Search tools, cards, skills, functions and keybindings for a term"
      - cmd: "doit show fd"
        desc: "Everything known about one subject, from every collection that has it"
      - cmd: "doit kit unresolved"
        desc: "Index rows naming something this machine cannot run"
    see_also: [doit-dashboard, doit-review, doit-labs, doit-workflows, toolbox]
    tags: [discovery, tools, search, attention]

  doit-dashboard:
    category: custom-tools
    description: "Cross-app glance behind doit dashboard — everything outstanding, in lanes"
    installed_via: uv
    usage: "doit dashboard [--lane <name>] [--json]"
    why_use: "One read-only glance at what is outstanding everywhere: tasks, habits, learning, reading, maintenance, project items, and what is approaching. Independent lanes, each ordered by its own app, so you pick by time and energy rather than following an invented ranking"
    examples:
      - cmd: "doit dashboard"
        desc: "Every lane, three rows each"
      - cmd: "doit dashboard --lane maintenance"
        desc: "One lane in more depth"
    see_also: [doit, doit-review, doit-labs, icb, learning]
    tags: [dashboard, overview, outstanding, glance, json]

  doit-review:
    category: custom-tools
    description: "Cadence register behind doit review — what to revisit and when"
    installed_via: uv
    usage: "doit review [due | list | done <id> | edit]"
    why_use: "Track recurring maintenance, skills, and tools to revisit; due dates derive from last-done plus cadence so there is nothing to hand-maintain. Last-done is observed from shell history and other tools' state files, so an item you did but never reported still counts"
    examples:
      - cmd: "doit review due"
        desc: "Show what's due now, most overdue first"
      - cmd: "doit review done brew-maintenance"
        desc: "Mark an item done, which advances its due date"
    see_also: [doit, doit-workflows]
    tags: [discovery, review, cadence]

  doit-labs:
    category: custom-tools
    description: "Hands-on practice deck behind doit labs — scheduled Labs and recall drills"
    installed_via: uv
    usage: "doit labs [due | list | show <id> | flash | done <id> | new <name>]"
    why_use: "Spaced hands-on practice: open a Lab to work through in one pane while you type in another, or run a flash recall drill from your tool examples. Schedule advances from last-practiced"
    examples:
      - cmd: "doit labs due"
        desc: "Show what's due to practice now"
      - cmd: "doit labs flash"
        desc: "Quick recall drill from your tool examples"
      - cmd: "doit labs done fzf-preview"
        desc: "Mark a Lab practiced, which advances its schedule"
    see_also: [doit, doit-review, doit-workflows]
    tags: [discovery, practice, spaced-repetition, labs]

  sesh:
    category: session-management
    description: "Smart tmux session manager with zoxide integration and fzf picker"
    installed_via: go-install
    usage: "sesh connect [session-name]"
    why_use: "Quick tmux session switching with zoxide directory tracking, fzf-based interactive picker, named session configs"
    examples:
      - cmd: "prefix + s"
        desc: "Open fzf session picker in tmux popup"
      - cmd: "sesh connect dotfiles"
        desc: "Connect to or create dotfiles session"
      - cmd: "sesh list"
        desc: "List all available sessions"
      - cmd: "sesh last"
        desc: "Switch to previously active session"
    see_also: [doit, tmux, tmuxinator]
    tags: [tmux, workflow, tui, go, zoxide]
    docs_url: "https://github.com/joshmedeski/sesh"

  notes:
    category: custom-tools
    description: "Interactive wrapper for zk note-taking with auto-discovery"
    installed_via: dotfiles
    usage: "notes [section]"
    why_use: "Quick access to zk notebook sections with gum menu interface"
    examples:
      - cmd: "notes"
        desc: "Open interactive menu (auto-discovers sections)"
      - cmd: "notes journal"
        desc: "Create journal entry"
      - cmd: "notes devnotes"
        desc: "Create dev note"
      - cmd: "notes learning"
        desc: "Create learning note"
    see_also: [zk, gum, doit]
    tags: [dotfiles, notes, zk, productivity]

  dectl:
    category: custom-tools
    description: "Data engineering control CLI for AWS Glue/Lambda/Step Functions pipelines"
    installed_via: uv
    usage: "dectl PIPELINE RESOURCE ACTION [ALIAS] [OPTIONS]"
    why_use: "Self-documenting control plane over your AWS DE pipelines — one {env}-templated config drives dev/staging/prod for glue, lambda, sfn, s3, and deploy"
    examples:
      - cmd: "dectl"
        desc: "Top-level help: available pipelines + active environment banner"
      - cmd: "dectl <pipeline> list"
        desc: "Show alias -> real AWS name mapping for a pipeline"
      - cmd: "dectl <pipeline> lambda deploy error-notifier"
        desc: "Fast local loop — update $LATEST and test directly"
      - cmd: "dectl <pipeline> lambda deploy error-notifier --publish"
        desc: "Publish an immutable version and move the configured alias"
      - cmd: "dectl <pipeline> sfn watch ingest"
        desc: "Tail the most recent Step Functions execution history"
      - cmd: "dectl --env prod <pipeline> list"
        desc: "Target the prod environment for a pipeline"
    see_also: [forge, syncer]
    tags: [aws, data-engineering, lambda, glue, step-functions, python, cli]
    docs_url: "https://github.com/datapointchris/dectl"

  bbkt:
    category: custom-tools
    description: "Pull request lifecycle for self-hosted Bitbucket Data Center, from the terminal"
    installed_via: go
    usage: "bbkt pr VERB [ID] [OPTIONS]"
    why_use: "Every maintained Bitbucket CLI targets Cloud's REST 2.0, which self-hosted Data Center does not speak — this drives create/review/merge without the web UI, inferring the repo from the git remote"
    examples:
      - cmd: "bbkt pr list --reviewing"
        desc: "What is waiting on your review, across every repo on the instance"
      - cmd: "bbkt pr create"
        desc: "Open a PR from the checked-out branch, title derived from the Jira key"
      - cmd: "bbkt pr view 42"
        desc: "Title, reviewers, approval state, and the command to diff it"
      - cmd: "git diff origin/main...HEAD | delta"
        desc: "Review the diff — the same merge-base diff the web view renders"
      - cmd: "bbkt pr approve"
        desc: "Approve the pull request for the current branch"
      - cmd: "bbkt pr merge 42"
        desc: "Merge after checking Bitbucket's own conflict and approval vetoes"
    see_also: [dectl, forge, delta]
    tags: [bitbucket, pull-request, code-review, jira, work, go, cli]
    docs_url: "https://github.com/datapointchris/bbkt"

  theme:
    category: custom-tools
    description: "Unified theme management across terminal apps (ghostty, tmux, btop, waybar, rofi, dunst)"
    installed_via: dotfiles
    usage: "theme <command> [theme-name]"
    why_use: "Apply consistent color schemes across all terminal and desktop apps from a single command"
    examples:
      - cmd: "theme list"
        desc: "List all available themes"
      - cmd: "theme preview"
        desc: "Interactive theme preview with fzf"
      - cmd: "theme apply kanagawa"
        desc: "Apply theme to all apps"
      - cmd: "theme current"
        desc: "Show currently applied theme"
      - cmd: "theme random"
        desc: "Apply a random theme"
      - cmd: "theme like"
        desc: "Mark current theme as liked"
    see_also: [font, btop, tmux]
    tags: [dotfiles, theming, customization, tui]

  font:
    category: custom-tools
    description: "Terminal font management with preview and cross-platform support"
    installed_via: dotfiles
    usage: "font <command> [font-name]"
    why_use: "Preview and apply terminal fonts across Ghostty and other terminals"
    examples:
      - cmd: "font list"
        desc: "List available monospace fonts"
      - cmd: "font preview"
        desc: "Interactive font preview with fzf"
      - cmd: "font apply 'JetBrainsMono Nerd Font'"
        desc: "Apply font to terminal"
      - cmd: "font current"
        desc: "Show current font"
      - cmd: "font random"
        desc: "Apply a random font"
    see_also: [theme]
    tags: [dotfiles, fonts, customization, terminal]

  forge:
    category: custom-tools
    description: "Run commands and dies (reusable scripts) across all git repos"
    installed_via: go-install
    usage: "forge <command> [args]"
    why_use: "Execute maintenance scripts and ad-hoc commands across your entire repo fleet with filtering, dry-run, and execution tracking"
    examples:
      - cmd: "forge exec -- git status --short"
        desc: "Run git status in every repo"
      - cmd: "forge exec -F dotfiles,homelab -- git pull"
        desc: "Pull specific repos"
      - cmd: "forge dies list"
        desc: "List available dies with descriptions and run stats"
      - cmd: "forge dies run maintenance/add-planning-to-gitignore.sh"
        desc: "Run a die across all repos"
      - cmd: "forge dies run maintenance/fix.sh -F dotfiles -n"
        desc: "Dry run a die on one repo"
      - cmd: "forge dies search planning"
        desc: "Search dies by name, description, or tags"
      - cmd: "forge dies stats"
        desc: "Show execution history"
    see_also: [syncer, toolbox]
    tags: [dotfiles, git, automation, workflow, go]
    docs_url: "https://github.com/datapointchris/forge"

  todoui:
    category: custom-tools
    description: "Personal project organization with local-first SQLite"
    installed_via: go-install
    usage: "todoui [command]"
    why_use: "Track projects, tasks, and items with a TUI interface backed by local SQLite, no server required"
    examples:
      - cmd: "todoui"
        desc: "Launch the TUI interface"
      - cmd: "todoui projects"
        desc: "List all projects"
      - cmd: "todoui items"
        desc: "List project items"
    see_also: [forge, toolbox]
    tags: [dotfiles, productivity, tui, sqlite, go]
    docs_url: "https://github.com/datapointchris/todoui"

  toolbox:
    category: custom-tools
    description: "CLI tool discovery and documentation browser"
    installed_via: dotfiles
    usage: "toolbox <command> [query]"
    why_use: "Explore and learn about installed CLI tools with examples and documentation"
    examples:
      - cmd: "toolbox"
        desc: "Interactive tool browser"
      - cmd: "toolbox list"
        desc: "List all registered tools"
      - cmd: "toolbox show ripgrep"
        desc: "Show details for specific tool"
      - cmd: "toolbox search git"
        desc: "Search tools by name or tag"
      - cmd: "toolbox random"
        desc: "Learn about a random tool"
      - cmd: "toolbox installed"
        desc: "Check which tools are installed"
    see_also: [lsalias, lsfunc]
    tags: [dotfiles, discovery, documentation, learning, go]

  doit-workflows:
    category: custom-tools
    description: "The reference cards — one tool's commands, or one goal carried through several"
    installed_via: uv
    usage: "doit workflows [list | show <id> | new <name> | random]"
    why_use: "Browse and search documented procedures for complex multi-step tasks. Cards live in the terminal-library checkout, so writing one never needs a release"
    examples:
      - cmd: "doit workflows list"
        desc: "Every card and what it covers"
      - cmd: "doit workflows show git-rebase"
        desc: "Render one card"
      - cmd: "doit workflows random"
        desc: "Render a card at random"
    see_also: [doit, notes]
    tags: [documentation, procedures, reference]

  aws-profiles:
    category: custom-tools
    description: "AWS profile switcher for managing multiple AWS accounts (macOS)"
    installed_via: dotfiles
    usage: "source aws-profiles"
    why_use: "Quickly switch between AWS profiles without editing config files"
    examples:
      - cmd: "source aws-profiles"
        desc: "Interactive profile selector (must be sourced)"
      - cmd: ". aws-profiles"
        desc: "Short form sourcing"
    see_also: [terraform]
    tags: [dotfiles, aws, cloud, macOS]
    notes: "Must be sourced (not executed) for environment variables to persist"

  indy:
    category: custom-tools
    description: "Semantic search index for local codebases, docs, and notes"
    installed_via: uv
    usage: "indy <command> [args]"
    why_use: "Local-first semantic search (sqlite-vec + ollama, no external APIs) across all your indexed repos and notes"
    examples:
      - cmd: "indy index"
        desc: "Index all active repos from ~/dev/repos.json"
      - cmd: "indy search 'query'"
        desc: "Semantic search across all indexed content"
      - cmd: "indy search 'query' --owned"
        desc: "Limit search to your own repos"
      - cmd: "indy status"
        desc: "Show index health and last run"
      - cmd: "indy stats"
        desc: "Bar charts of files/chunks per repo"
    see_also: [relate, toolbox, ripgrep]
    tags: [search, semantic, embeddings, sqlite, ollama, mcp]
    docs_url: "https://github.com/datapointchris/indy"

  logsift:
    category: custom-tools
    description: "LLM-optimized log analysis and command monitoring for agentic workflows"
    installed_via: uv
    usage: "logsift monitor -- <command>"
    why_use: "Run verbose commands and get structured, minimal error extraction instead of dumping thousands of lines into the LLM context"
    examples:
      - cmd: "logsift monitor -- bash install.sh"
        desc: "Run a command and analyze its output for issues"
      - cmd: "logsift monitor -- task test"
        desc: "Monitor a long-running task and surface errors"
    see_also: [forge, bat]
    tags: [logs, monitoring, agentic, claude-code, python]
    docs_url: "https://github.com/datapointchris/logsift"

  packages:
    category: custom-tools
    description: "Query packages.yml for browsing and discovery"
    installed_via: dotfiles
    usage: "packages <command> [filters]"
    why_use: "Browse, search, and filter the dotfiles package manifest by section, tag, and platform"
    examples:
      - cmd: "packages list --tag cli"
        desc: "List all CLI packages"
      - cmd: "packages search ripgrep"
        desc: "Search packages by name"
      - cmd: "packages show fzf"
        desc: "Show package details"
      - cmd: "packages verify"
        desc: "Check drift between packages.yml, manifests, and scripts"
      - cmd: "packages stats"
        desc: "Show package counts"
    see_also: [toolbox, doit]
    tags: [dotfiles, packages, discovery, yaml]

  relate:
    category: custom-tools
    description: "Claude Code-focused information management and cross-content synthesis — analyzes YouTube videos, articles, and URLs"
    installed_via: uv
    usage: "relate <command> [args]"
    why_use: "Ingest content you can't read manually, store structured analyses in SQLite, and synthesize across them with interactive Claude sessions"
    examples:
      - cmd: "relate analyze 'https://example.com/article'"
        desc: "Analyze a URL, save to DB, display via glow"
      - cmd: "relate analyze 'https://example.com' --discuss"
        desc: "Analyze then open an interactive analytical session"
      - cmd: "relate batch urls.txt"
        desc: "Batch analyze and open a meta-analysis session"
    see_also: [indy, toolbox]
    tags: [synthesis, analysis, claude-code, sqlite, mcp, youtube, video, transcript, article, url]
    docs_url: "https://github.com/datapointchris/relate"

  syncer:
    category: custom-tools
    description: "Check if local git repos are fully synced before switching machines"
    installed_via: uv
    usage: "syncer [command]"
    why_use: "Fetch each configured repo, detect uncommitted/unpushed/behind/stashed state, and auto-pull or auto-push when safe"
    examples:
      - cmd: "syncer"
        desc: "Check sync status of all configured repos"
      - cmd: "syncer update"
        desc: "Update syncer to the latest release"
    see_also: [forge, git]
    tags: [git, sync, multi-machine, python]
    docs_url: "https://github.com/datapointchris/syncer"

  webviewrs:
    category: custom-tools
    description: "Open a URL in a minimal native webview window"
    installed_via: cargo
    usage: "webviewrs [options] <url>"
    why_use: "Chromeless native window for a single web app with persistent login storage and dark mode - no browser tabs or clutter"
    examples:
      - cmd: "webviewrs https://ichrisbirch.com"
        desc: "Open a URL in a bare webview window"
      - cmd: "webviewrs --dark --title Dashboard https://grafana.local"
        desc: "Dark mode with a custom window title"
      - cmd: "webviewrs -n immich https://immich.local"
        desc: "Named app with persistent storage for logins"
    tags: [webview, gui, rust, cargo]
    docs_url: "https://github.com/datapointchris/webviewrs"

  zmk:
    category: custom-tools
    description: "Build, draw, check and flash a ZMK keyboard config"
    installed_via: dotfiles
    usage: "zmk <command>"
    why_use: "Incremental firmware builds from a standard build.yaml with isolated per-repo west workspaces, plus keymap alignment, SVG rendering and flashing from one place"
    examples:
      - cmd: "zmk build"
        desc: "Build every target in build.yaml"
      - cmd: "zmk build --pristine"
        desc: "Force a clean rebuild of all targets"
      - cmd: "zmk sync"
        desc: "Align the keymap, redraw the SVG, then build"
      - cmd: "zmk flash"
        desc: "Write firmware to a half, picking the target with fzf"
      - cmd: "zmk check"
        desc: "Report what is missing or has drifted"
    tags: [zmk, firmware, keyboard, build]

  # SHELL UTILITY FUNCTIONS
  # =================================================================

  # Info/Listing Functions
  commithelp:
    category: custom-tools
    description: "Suggest commit type based on staged files"
    installed_via: dotfiles
    usage: "commithelp"
    why_use: "Get intelligent suggestions for conventional commit types based on file patterns"
    examples:
      - cmd: "commithelp"
        desc: "Analyze staged files and suggest commit type"
    see_also: [lscommits]
    tags: [git, commits, automation]

  formatting_demo:
    category: shell-formatting
    description: "Display modular demo of formatting functions by type"
    installed_via: dotfiles
    usage: "formatting_demo [all|titles|headers|sections|banners|status|colors|utilities]"
    why_use: "See visual examples of formatting options - view all or filter by type"
    examples:
      - cmd: "formatting_demo"
        desc: "Show usage menu"
      - cmd: "formatting_demo all"
        desc: "Show complete demo of all formatting functions"
      - cmd: "formatting_demo titles"
        desc: "Show only title variants and colors"
      - cmd: "formatting_demo banners"
        desc: "Show only banner variants and colors"
      - cmd: "formatting_demo status"
        desc: "Show status message functions"
    see_also: [allcolors, toolbox]
    tags: [shell, formatting, demo, reference]

  allcolors:
    category: shell-formatting
    description: "Display all available ANSI color codes with examples"
    installed_via: dotfiles
    usage: "allcolors"
    why_use: "Quick visual reference for all standard and bright colors available in shell"
    examples:
      - cmd: "allcolors"
        desc: "Show all colors: black, red, green, yellow, blue, magenta, cyan, white, and bright variants"
    see_also: [formatting_demo, print_banner]
    tags: [shell, colors, demo, reference]

  # Color Functions
  fad:
    category: version-control
    description: "Git add modified files matching pattern"
    installed_via: dotfiles
    usage: "fad <pattern>"
    why_use: "Quickly stage modified files by name pattern using grep"
    examples:
      - cmd: "fad init"
        desc: "Stage all modified files with 'init' in name"
      - cmd: "fad test"
        desc: "Stage all modified test files"
    see_also: [git, git-forgit-add]
    tags: [git, staging, workflow]

  gm:
    category: version-control
    description: "Git commit with message"
    installed_via: dotfiles
    usage: "gm <message>"
    why_use: "Quick commit shorthand"
    examples:
      - cmd: 'gm "feat: add new feature"'
        desc: "Create commit with message"
    see_also: [git]
    tags: [git, commit]

  git-old-branches:
    category: version-control
    description: "Find old branches merged into master with no recent commits"
    installed_via: dotfiles
    usage: "git-old-branches [--remote]"
    why_use: "Identify stale branches for cleanup (older than 1 month)"
    examples:
      - cmd: "git-old-branches"
        desc: "Find old local branches"
      - cmd: "git-old-branches --remote"
        desc: "Find old remote branches"
    see_also: [git, git-forgit-branch-delete]
    tags: [git, cleanup, branches]

  # File/Directory Management Functions
  findup:
    category: file-management
    description: "Find file or directory by searching up the directory tree"
    installed_via: dotfiles
    usage: "findup <name>"
    why_use: "Search for files/dirs in parent directories (e.g., find .git, package.json)"
    examples:
      - cmd: "findup .git"
        desc: "Find git root directory"
      - cmd: "findup package.json"
        desc: "Find nearest package.json"
    see_also: [fd, find]
    tags: [file-search, navigation]

  mkd:
    category: file-management
    description: "Create a new directory and cd into it"
    installed_via: dotfiles
    usage: "mkd <directory>"
    why_use: "Shortcut for mkdir -p && cd in one command"
    examples:
      - cmd: "mkd ~/projects/new-project"
        desc: "Create directory and enter it"
    see_also: [mkdir, cd]
    tags: [directory, navigation]

  cl:
    category: file-management
    description: "Move to directory and list contents"
    installed_via: dotfiles
    usage: "cl <directory>"
    why_use: "Combine cd and ls in one command"
    examples:
      - cmd: "cl ~/projects"
        desc: "Change to directory and list files"
    see_also: [cd, eza]
    tags: [directory, navigation]

  touchdate:
    category: file-management
    description: "Create file with timestamp prefix"
    installed_via: dotfiles
    usage: "touchdate <filename>"
    why_use: "Create timestamped files (YYYY-MM-DD_HHMMSS-filename)"
    examples:
      - cmd: "touchdate notes.md"
        desc: "Creates 2025-11-09_143022-notes.md"
    see_also: [touch, date]
    tags: [file-creation, timestamp]

  sizeof:
    category: file-management
    description: "Determine size of file or total size of directory"
    installed_via: dotfiles
    usage: "sizeof [path]"
    why_use: "Human-readable file/directory sizes"
    examples:
      - cmd: "sizeof"
        desc: "Show sizes of all files in current directory"
      - cmd: "sizeof ~/projects"
        desc: "Show total size of directory"
    see_also: [du, dust]
    tags: [file-info, disk-usage]

  extract:
    category: file-management
    description: "Auto-extract any archive type (tar, zip, gz, etc.)"
    installed_via: dotfiles
    usage: "extract <archive>"
    why_use: "Universal extraction without remembering archive-specific commands"
    examples:
      - cmd: "extract file.tar.gz"
        desc: "Extract tarball"
      - cmd: "extract archive.zip"
        desc: "Extract zip file"
    see_also: [tar, unzip]
    tags: [archive, extraction, compression]

  gzipsize:
    category: file-management
    description: "Compare original and gzipped file size"
    installed_via: dotfiles
    usage: "gzipsize <file>"
    why_use: "See compression ratio before actually compressing"
    examples:
      - cmd: "gzipsize large-file.txt"
        desc: "Show compression savings"
    see_also: [gzip]
    tags: [compression, file-info]

  # System/Process/Network Functions
  ubuntu-docker:
    category: containerization
    description: "Create new Ubuntu Docker container and ssh into it"
    installed_via: dotfiles
    usage: "ubuntu-docker"
    why_use: "Quick disposable Ubuntu environment for testing"
    examples:
      - cmd: "ubuntu-docker"
        desc: "Launch and enter Ubuntu container"
    see_also: [docker, orbstack]
    tags: [docker, ubuntu, testing]

  pkill:
    category: system-monitoring
    description: "Kill process by name using interactive fzf selection"
    installed_via: dotfiles
    usage: "pkill"
    why_use: "Safely select and kill processes with preview"
    examples:
      - cmd: "pkill"
        desc: "Launch interactive process selector"
    see_also: [ps, fzf, htop]
    tags: [process, kill, fzf, interactive]

  listening:
    category: system-monitoring
    description: "List applications listening on ports"
    installed_via: dotfiles
    usage: "listening [pattern]"
    why_use: "Find what's using specific ports or show all listening services"
    examples:
      - cmd: "listening"
        desc: "Show all listening ports"
      - cmd: "listening 3000"
        desc: "Find what's on port 3000"
    see_also: [lsof, netstat]
    tags: [network, ports, debugging]

  hosts:
    category: system-monitoring
    description: "Display /etc/hosts file with syntax highlighting"
    installed_via: dotfiles
    usage: "hosts [pattern]"
    why_use: "Quick view of host file entries"
    examples:
      - cmd: "hosts"
        desc: "Show all host entries"
      - cmd: "hosts local"
        desc: "Search for localhost entries"
    see_also: [bat]
    tags: [network, dns, hosts]

  resetroute:
    category: system-monitoring
    description: "Reset network and flush routing table"
    installed_via: dotfiles
    usage: "resetroute"
    why_use: "Fix network connectivity issues by resetting interfaces"
    examples:
      - cmd: "resetroute"
        desc: "Flush routes and reset network"
    tags: [network, troubleshooting, macOS]

  external-ip:
    category: system-monitoring
    description: "Get external/public IP address"
    installed_via: dotfiles
    usage: "external-ip"
    why_use: "Quick check of public IP from terminal"
    examples:
      - cmd: "external-ip"
        desc: "Show your public IP"
    see_also: [local-ip, curl]
    tags: [network, ip, public]

  local-ip:
    category: system-monitoring
    description: "Get local IP address"
    installed_via: dotfiles
    usage: "local-ip"
    why_use: "Find your local network IP"
    examples:
      - cmd: "local-ip"
        desc: "Show local IP address"
    see_also: [external-ip, ifconfig]
    tags: [network, ip, local]

  all-local-ips:
    category: system-monitoring
    description: "Show all local IP addresses (IPv4 and IPv6)"
    installed_via: dotfiles
    usage: "all-local-ips"
    why_use: "See all network interfaces and their IPs"
    examples:
      - cmd: "all-local-ips"
        desc: "List all IPs"
    see_also: [local-ip, ifconfig]
    tags: [network, ip]

  ifactive:
    category: system-monitoring
    description: "Show only active network interfaces"
    installed_via: dotfiles
    usage: "ifactive"
    why_use: "Filter ifconfig to show only active connections"
    examples:
      - cmd: "ifactive"
        desc: "List active network interfaces"
    see_also: [ifconfig, local-ip]
    tags: [network, interfaces]

  colored-log:
    category: text-processing
    description: "Tail log file with colored output by log level"
    installed_via: dotfiles
    usage: "colored-log <logfile>"
    why_use: "Colorize log levels (ERROR, WARNING, INFO, DEBUG) for better readability"
    examples:
      - cmd: "colored-log app.log"
        desc: "Tail log with colors"
    see_also: [tail, bat]
    tags: [logs, monitoring, tail]

  server:
    category: file-viewer
    description: "Start HTTP server on port 2222 and open browser"
    installed_via: dotfiles
    usage: "server"
    why_use: "Quick local file server for testing static sites"
    examples:
      - cmd: "server"
        desc: "Serve current directory on http://localhost:2222"
    see_also: [python]
    tags: [http, server, testing]

  digga:
    category: system-monitoring
    description: "Run dig and display most useful DNS information"
    installed_via: dotfiles
    usage: "digga <domain>"
    why_use: "Simplified dig output for common DNS lookups"
    examples:
      - cmd: "digga google.com"
        desc: "Get DNS info for domain"
    see_also: [dig, nslookup]
    tags: [dns, network, debugging]

  # Development/Language Functions
  checknode:
    category: language-manager
    description: "Display node and npm version and location"
    installed_via: dotfiles
    usage: "checknode"
    why_use: "Verify which node/npm version is active (useful with nvm)"
    examples:
      - cmd: "checknode"
        desc: "Show node and npm info"
    see_also: [nvm, node, npm]
    tags: [node, npm, version]

  venv:
    category: language-manager
    description: "Activate Python venv by searching up directory tree"
    installed_via: dotfiles
    usage: "venv"
    why_use: "Auto-find and activate .venv in current or parent directories"
    examples:
      - cmd: "venv"
        desc: "Activate nearest Python venv"
    see_also: [uv, python, findup]
    tags: [python, venv, activation]

  pytestloop:
    category: automation
    description: "Run pytest continuously in a loop"
    installed_via: dotfiles
    usage: "pytestloop"
    why_use: "Watch mode for pytest (activates venv, runs continuously)"
    examples:
      - cmd: "pytestloop"
        desc: "Start continuous testing"
    see_also: [pytest, venv]
    tags: [python, testing, automation]

  package-lambda:
    category: automation
    description: "Package Python Lambda function with dependencies"
    installed_via: dotfiles
    usage: "package-lambda <function.py> [requirements.txt]"
    why_use: "Create deployment package for AWS Lambda"
    examples:
      - cmd: "package-lambda handler.py"
        desc: "Package function with requirements"
      - cmd: "package-lambda handler.py no-requirements"
        desc: "Package without dependencies"
    see_also: [make-lambda-layer, python]
    tags: [aws, lambda, python, deployment]

  make-lambda-layer:
    category: automation
    description: "Create AWS Lambda layer with Python packages"
    installed_via: dotfiles
    usage: "make-lambda-layer <layer-name> <packages...>"
    why_use: "Build Lambda layer for shared dependencies"
    examples:
      - cmd: "make-lambda-layer requests-layer requests boto3"
        desc: "Create layer with packages"
    see_also: [package-lambda, python]
    tags: [aws, lambda, python, deployment]

  printcolors:
    category: custom-tools
    description: "Display ANSI color palette"
    installed_via: dotfiles
    usage: "printcolors"
    why_use: "Quick reference for ANSI color codes and testing terminal colors"
    examples:
      - cmd: "printcolors"
        desc: "Display full color palette"
    tags: [dotfiles, terminal, colors]

  packup:
    category: custom-tools
    description: "Create compressed backup archive of specified directories with progress tracking"
    installed_via: dotfiles
    usage: "packup -n <name> -d <dest> <dir1> [dir2...]"
    why_use: "One-off compressed backups with smart exclusions, live progress, and comprehensive statistics"
    examples:
      - cmd: "packup -n dotfiles -d ~/Documents .claude notes learning"
        desc: "Backup directories to ~/Documents"
      - cmd: "packup -n learning-docs -d ~/Documents learning"
        desc: "Create named backup"
      - cmd: "packup -n configs -d ~/Backups .config dotfiles"
        desc: "Backup to custom destination"
    see_also: [safekeep, tar, fd]
    tags: [backup, compression, progress, one-off]
    notes: "Excludes .venv, node_modules, caches but keeps .git. Uses multi-threaded zstd compression by default."
    docs_url: "docs/apps/backup.md"

  safekeep:
    category: custom-tools
    description: "Config-driven file preservation with self-describing dated snapshots - rsync-based backup and restore"
    installed_via: uv
    usage: "safekeep <backup|snapshots|restore|config|init> [OPTIONS]"
    why_use: "Back up scattered config files, scripts, and git-untracked WIP to a network drive, and restore them onto a rebuilt machine with file modes intact"
    examples:
      - cmd: "safekeep backup"
        desc: "Run backup using auto-detected config"
      - cmd: "safekeep backup --dry-run"
        desc: "Preview what would be copied"
      - cmd: "safekeep snapshots"
        desc: "List snapshots at the destination"
      - cmd: "safekeep restore --to /tmp/restore-test"
        desc: "Rehearse a restore, picking snapshot and groups with fzf"
      - cmd: "safekeep restore --to / --tag wip"
        desc: "Restore every group tagged 'wip'"
    see_also: [packup, rsync, fzf]
    tags: [backup, restore, rsync, config-driven, snapshots, network]
    notes: "Backup is Python stdlib only; restore shells out to fzf for interactive selection. Config at ~/.config/safekeep/<name>.json. Each snapshot carries a manifest recording groups, tags, and file modes, so it restores without the config that made it. Snapshots are never pruned. Bare `safekeep` prints usage — nothing writes without an explicit verb."
    docs_url: "docs/apps/safekeep.md"

  refcheck:
    category: custom-tools
    description: "Find broken file references, validate variable paths, and detect fragile path patterns"
    installed_via: uv
    usage: "refcheck [OPTIONS] [path]"
    why_use: "Proactive error detection before running tests - catches broken paths in seconds vs minutes of test failures"
    examples:
      - cmd: "refcheck"
        desc: "Validate all file references in current directory"
      - cmd: "refcheck install/"
        desc: "Check specific directory for broken references"
      - cmd: "refcheck --pattern 'old/path/' --desc 'Now new/path/'"
        desc: "Find old pattern after refactoring"
      - cmd: "refcheck --type sh --skip-docs"
        desc: "Check only shell scripts, skip documentation"
      - cmd: "refcheck --strict"
        desc: "Treat warnings as errors (strict mode for CI)"
      - cmd: "refcheck --learn-rules"
        desc: "Learn path mappings from git rename history"
    see_also: [grep, fd]
    tags: [validation, testing, refactoring, ci]
    notes: "Resolves variable paths ($SCRIPT_DIR, $DOTFILES_DIR). Learns from git history to suggest correct paths. Config at ~/.config/refcheck/config.toml."
    docs_url: "docs/apps/refcheck.md"

  brew-maintenance:
    category: custom-tools
    description: "Run full Homebrew maintenance — update, upgrade, cleanup, autoremove"
    installed_via: dotfiles
    usage: "brew-maintenance"
    why_use: "Single command to keep Homebrew fully up to date and cleaned up without running four separate commands"
    examples:
      - cmd: "brew-maintenance"
        desc: "Update, upgrade, cleanup, and autoremove all Homebrew packages"
    see_also: [brew]
    tags: [brew, homebrew, maintenance, packages, macos]

  layers:
    category: custom-tools
    description: "Browse ZMK keyboard layers interactively — pick from fzf, render SVG in terminal"
    installed_via: dotfiles
    usage: "layers"
    why_use: "Visually inspect keyboard layout without opening a browser — picks layer via fzf and renders the SVG with chafa"
    examples:
      - cmd: "layers"
        desc: "Open fzf layer picker and render selected layer as ASCII art"
    see_also: [yq, gum, chafa]
    tags: [keyboard, zmk, fzf, visualization, chafa, hardware]

  reload-dev-forever:
    category: custom-tools
    description: "Reload nginx and supervisor in a loop every 15 seconds"
    installed_via: dotfiles
    usage: "reload-dev-forever"
    why_use: "Keep dev services reloading continuously during active development without manual intervention"
    examples:
      - cmd: "reload-dev-forever"
        desc: "Start looping nginx + supervisor reload every 15 seconds"
    see_also: []
    tags: [nginx, supervisor, dev, automation, loop]

  yt-dlp:
    category: media
    description: "Download video/audio and subtitles from YouTube and 1000+ other sites"
    installed_via: brew
    usage: "yt-dlp [options] <url>"
    why_use: "Fetch a YouTube transcript without a browser: --write-auto-subs/--write-subs pull captions (json3 avoids the rolling-duplicate lines VTT emits), --skip-download grabs only the text"
    examples:
      - cmd: "yt-dlp --list-subs --skip-download <url>"
        desc: "List available subtitle/caption languages for a video"
      - cmd: "yt-dlp --skip-download --write-auto-subs --sub-langs en --sub-format json3 -o '%(id)s.%(ext)s' <url>"
        desc: "Download English auto-captions as structured json3"
    see_also: [yt-transcript, relate, jq, ffmpeg]
    tags: [youtube, video, audio, transcript, captions, subtitles, download, media]

  yt-transcript:
    category: media
    description: "Print a YouTube video's transcript as plain text to stdout"
    installed_via: dotfiles
    usage: "yt-transcript <youtube-url> [lang]"
    why_use: "Unix-friendly transcript extraction — wraps yt-dlp + jq to emit clean plain text you can pipe into claude, a file, or any other tool. Prefers human captions, falls back to auto-generated"
    examples:
      - cmd: "yt-transcript 'https://youtu.be/VIDEO' | claude -p 'turn this into a step-by-step workflow'"
        desc: "Summarize a tutorial into a workflow in a specified style"
      - cmd: "yt-transcript 'https://youtu.be/VIDEO' > notes.txt"
        desc: "Save the raw transcript to a file"
    see_also: [yt-dlp, relate, jq]
    tags: [youtube, video, transcript, captions, subtitles, summarize, media, pipe]

# =================================================================
# Metadata
# =================================================================

categories:
  - file-viewer
  - file-management
  - search
  - text-processing
  - navigation
  - version-control
  - editor
  - terminal-multiplexer
  - session-management
  - language-manager
  - linter-formatter
  - language-server
  - containerization
  - infrastructure
  - automation
  - system-monitoring
  - shell-formatting
  - media
  - custom-tools

install_methods:
  - brew: "Homebrew package manager (macOS/Linux)"
  - cargo: "Rust package manager"
  - npm: "Node.js package manager (via nvm)"
  - uv: "Python package manager"
  - script: "Installation script"
  - cask: "Homebrew cask (GUI applications)"
  - dotfiles: "Custom tools included in dotfiles repository"
